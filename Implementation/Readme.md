Main Entry Point: Use code.ipynb as the primary notebook to run all experiments.

Data Loading:
-> For Wolf–Sheep and Flocking experiments (NetLogo-generated data): Import and use behaviorspace_all_runs.py to load simulation data.
-> For the GARCH model experiment: Import and use wide_garch_loader.py to load the dataset.

Core Module: Import conceptual_model_module.py for all experiments, as it is required across all configurations
